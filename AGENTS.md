# AGENTS.md

本文件只记录长期适用的项目约定，不维护插件清单、功能介绍、安装步骤、当前版本或命令清单。这些信息以 [README.md](README.md)、相关组件文档和 package manifest 为准。仅在项目约定变化时修改本文件，不因增删或升级单个插件而更新。

## 项目边界

- DeepSeek Harness 官方源码保持零私改。通过公开扩展点、profile bundle 或独立应用壳提供功能，不修改上游主仓或补丁修改已安装的官方模块。
- 缺少公开扩展点时说明限制并请求用户决定，不伪造上游 API、不自行扩大修改范围。
- 插件与配套应用统一在此仓库管理，各组件独立配置、测试、版本化和发布。
- 官方源码版本与部署运行时版本分开管理。兼容性必须有实际验证，不自动跟随 latest 或扩大声明范围。
- 代码交付不等于用户部署；发布变更不自动授权更新现有安装、迁移数据或启用外部服务。
- GitHub 公开仓是脱敏导出，不是第二开发源。只在本仓库改代码，用导出脚本同步当前工作区；不把内网 Git 主机名写入导出树，也不导出 `aidev`。

## 代码组织与开发

- 插件放在 `plugins/`，应用壳放在 `apps/`，运行时依赖定义放在 `runtime/`，工程脚本放在 `scripts/`，说明与决策放在 `docs/`。
- 每个组件拥有自己的实现、配置和测试，声明真实依赖与公开导出；不得跨组件引用私有源码。共享实现只为多个真实消费者提取。
- 保持独立运行时与开发 workspace 的依赖管理隔离，不随意合并锁文件。
- TypeScript 使用 ESM 和严格类型，保持所在包的导入与构建约定。不得通过弱化类型或检查来掩盖 API 不兼容。
- 注册、监听、定时器和后台工作必须有生命周期所有者及清理机制；卸载停止接收新工作，并等待已拥有的工作结束。
- 在配置、外部请求、文件、持久化数据和进程通信入口校验输入；保持最小权限与拒绝默认值，不绕过权限或安全检查。
- UI 文案通过组件的本地化机制管理。注释说明非显然的行为、失败条件和所有权，不复述代码或记录开发过程。
- 行为变更同步相关文档与测试。重要架构决策记录在 `docs/decisions/`，同一事实只维护一个权威出处。

## 安装与数据安全

- 安装器只操作明确归属自己的目录，防止并发写入；重复执行和失败恢复不得覆盖用户配置。
- 不擅自删除未知目录、清除残留锁、终止已有进程或覆盖现有安装；涉及用户数据和服务时先核实归属及授权。
- 保持依赖锁定、发布包完整性校验及安装脚本执行限制，不通过关闭保护来解决安装失败。
- 凭据通过专用服务或引用管理，不写入源码、发布包、日志、截图或回复。外部授权、真实调用和数据迁移必须在用户授权范围内。
- 测试使用隔离目录和资源，不将用户 profile 或真实账号作为默认夹具。

## 验证与交付

- 从根目录及受影响组件的 package manifest 选择最小相关检查；纯文档修改不重复运行无关构建。
- 行为变更增加回归测试；安装、导出、依赖或运行时变更还须验证构建后的发布包与真实启动入口，不能只验证源码或 mock。
- 并发测试使用独立资源和状态同步，清理等待资源结束；不靠固定睡眠、盲目重试或放宽断言掩盖问题。
- 明确区分通过、跳过与未验证；无密钥测试不能代替真实账号验收，非目标平台测试不能证明目标平台可用。
- 发布时同步包版本、构建产物与目录元数据，校验包身份、兼容范围、大小和摘要；不得用不同内容覆盖已发布的同版本包。
- 提交前检查工作区与暂存 diff，保留用户无关改动并执行 `git diff --cached --check`；不提交依赖目录、缓存、临时安装或凭据。
- 仅在用户要求的范围内提交、推送或创建 PR；正常推送并核对远端，不默认强推、自动合并或创建额外仓库。
