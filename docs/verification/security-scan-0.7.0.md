# Security Scan 0.7.0 验证

日期：2026-09-10；macOS arm64，官方 DSH 0.1.0-rc.8，App 内置 Node 24.19.0。

## 自动化回归

- 插件 66 项测试通过，无跳过；包含真实 Semgrep、官方 profile loader、原生 Agent Loop/ToolRuntime/session 事件、双入口、默认通知、取消、预算、部分结果、引用/路径约束、环境工具隔离和会话生命周期。
- TypeScript 检查与服务端/客户端构建通过。固定的运行时锁文件没有修改。最终 catalog 验证包身份、大小、SHA-256 及全部 60 个文件；运行时文件与 UI 验收安装逐字节一致，最终包重新安装并通过正式 profile 加载入口。
- preset 安装验证重复/并发调用、归属和摘要校验，保留同名未知目录、用户修改和符号链接；不改变官方模式或用户默认模式。

## 真实运行时验收

发布包安装到独立 DSH profile，在 3198 端口通过正式 Web 入口启动。模型使用本机合成 HTTP SSE 服务与官方 DeepSeek 适配器，扫描目标为隔离的 Python、Go、JavaScript 三文件夹具，不调用用户真实模型或扫描业务仓库。

工作台使用 Semgrep 检出 3 个源码候选；原生 Agent 执行 plan_review → read_evidence → submit_review → finish_review，保留 3 项待确认结论与精确源码引用，生成原始规则报告及 AI 复核 JSON/HTML。模式在原生选择器和执行会话中显示为“安全审计”；会话包含规则阶段、实际工具调用及报告结果。

测试中发现并修复两项真实接线问题：后台 factory setup 需从声明注入的上下文捕获 preset 服务；完成后不能立即销毁 session，否则客户端会将它移出目录。前者由真实 preset 加载验证，后者有原生所有权生命周期回归测试；最终发布包在不刷新页面的情况下通过“打开会话”验收。

从标准模式原生对话发送扫描请求，security_start_scan 创建 conversation 运行；该运行使用独立的安全审计 preset，再次完成同样四步、复核同样 3 个候选。原会话保持标准模式并收到准备、扫描和 AI 活动通知；工作台显示运行已完成，HTML 预览包含规则 3/3、AI 四轮、源码引用及 0.7.0 版本。两个入口均未改变原会话的默认 preset。

本记录验证流程及扩展点兼容性，不证明真实模型的漏洞发现质量，也不包含大型上下文触发压缩、benchmark、漏洞动态复现、业务仓库完整覆盖或新增 uv.lock 支持。原来的用户实例及 ~/.dsh 未更新。

验收结束后关闭本次浏览器页，停止本次拥有的 3198 Host 和合成模型服务，确认任务锁释放，并恢复隔离 profile 原设置。旧 3196 测试实例与用户正式安装均保留。
