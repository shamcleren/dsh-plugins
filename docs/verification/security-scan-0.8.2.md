# Security Scan 0.8.2 报告可读性验证

2026-09-10，macOS arm64，官方 DSH 0.1.0-rc.8。

读取用户提供的 HTML 及对应规范 JSON（129fe978-8def-407d-95df-d4ddde8e7ad8），统计 525 条原始发现：源码 11 条、凭据 13 条、依赖 501 条；源码中 2 条标记风险、2 条待复核，凭据 4 条待核实，依赖涉及 38 个包。另有 7 条历史凭据排除结论引用脱敏占位符，需要复查。没有修改这些历史状态或原始文件。

新模板以中文处理清单为默认入口，源码同类位置集中显示、依赖按包展示声明位置和公告修复版本；详细原文、重复公告引用、模型用量和执行明细折叠。别名组优先选择非空摘要，修复建议去掉不同公告链接造成的重复正文。已排除项默认不进入详情筛选，但可以查看。新的原生 AI 提示词要求中文结论；当取证工具确实遮蔽了对应行的凭据原值时，拒绝 dismissed 提交，要求保留待复核。

TypeScript 与构建通过。完整回归 78 项通过，无跳过（含真实 Semgrep、官方 profile 入口与原生 Agent）。新增回归验证分类统计、脱敏结论提醒、非空别名摘要、默认筛选、内联脚本语法以及原生 Agent 对脱敏凭据错误排除的拒绝与纠正。最后的摘要导航按钮改动再次通过报告测试。

将构建包安装到归属明确的独立 profile，以 3198 正式 Web 入口打开合成报告；验证首页只显示处理清单、运行明细与原始记录默认折叠，从“查看该包详情”自动展开并筛选对应包，3 条合成公告归并为 2 项。没有启动扫描或真实模型。历史真实数据仅在拥有的可视化目录生成新版 HTML 预览，没有经网络提供给其他服务。

包内 68 个文件与构建及最终测试安装逐字节一致（package.json 比较 JSON 语义），catalog 大小与摘要通过。SHA-256：f9e17a8b2876315ffa77f349d69f09510ffa3e6e8dd8d99a1cce54acabe0e49f。

验收完成关闭本次页面并停止拥有的 Host。用户正式 App、原始报告及业务仓库均未修改；没有提交或推送本次变更。
