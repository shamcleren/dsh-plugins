# Security Scan 0.8.0 验证

日期：2026-09-10；macOS arm64，官方 DSH 0.1.0-rc.8，App 内置 Node 24.19.0。

## 回归与发布包

TypeScript 检查、服务端与客户端构建通过；75 项测试全部通过，无跳过。覆盖真实 Semgrep 三语言规则、超过 1500 文件的批次、Git 标准忽略规则与 tracked ignored 文件、暂存区快照、别名合并、OSV 详情修复版本、HTML 转义与链接、私有 JSON 检查点、终态单份报告、原生 Agent 完成 / 取消 / 错误和双入口流程、官方 profile 加载入口。

0.8.0 包共 64 个文件，201169 bytes，SHA-256：`875601fb2ceddad3596801dfcc6418520e6f0e37246249b88432b9df80927f4f`。包内容与构建文件、独立 profile 中安装内容一致（package.json 比较 JSON 语义，其他文件逐字节比较）；marketplace 中版本、大小和摘要一致。新增公开 webServer 服务仅用于生成当前 DSH 报告工作台链接，未修改上游模块。

## 独立 Host 与 UI

在已归属的临时 profile 中以 3198 端口启动正式 Web 入口，使用本机合成 SSE 模型及 Python / Go / JS 三文件夹具。工作台执行时报告数保持不变，结束后仅新增一份最终 JSON + HTML。原生执行顺序为 plan_review → read_evidence → submit_review → finish_review → 模型最终文字，实际 5 轮，原生 completed；检查点工具未强制结束。保留 3 项待复核候选、3/3 源码规则覆盖，未宣称存在已确认漏洞。

浏览器实测报告分组、展开详情；点击 app.py:4 后由 RPC 读取源码，正确高亮第 4 行，页面无横向溢出。官方消息 Markdown 不接受 file:// 链接，因此预留最终报告 ID，通过当前 Host 的工作台 URL 导航。最终模型回复的“查看扫描报告”是实际超链接，点击后在当前页面打开对应报告。最终验收报告 ID：b7611cbc-2a75-4ec4-a035-15f1ca6534b1。

源码读取会验证报告中允许的位置及内容摘要，拒绝过期证据；独立 HTML 的编辑器协议链接未在本次测试中启动真实 VS Code / Cursor。历史真实报告的本地 HTML 自动打开被浏览器 URL 策略拒绝，没有绕过；合成报告在独立 DSH 工作台内完成交互验收。

## 真实仓库与历史报告

只读扫描用户此前指定的 bk-aidev 工作区，没有执行项目脚本或调用真实模型。Semgrep 1.176.1 完成 1729/1729 个支持语言源文件，检出 11 条规则候选。快照仍有 6 个超过 1 MiB 的非源码文件：两个 tokenizer cache、三张图片、一份文档；报告逐项列出。此次关闭依赖和密钥引擎，因此不作为三引擎全量验收。

历史报告的 mistune@3.1.4 共 30 条公告记录。读取官方 OSV 详情后，根据明确别名合并为 16 项漏洞；详情全部成功取得。公告列出 3.2.1 / 3.3.0 等修复版本，展示时说明维护分支与其他公告差异，不认定任一版本自动修复所有问题。基于历史 JSON 生成独立展示预览，没有重写原始 JSON / HTML，也没有重新作出 AI 漏洞结论。

限制仍显式保留：不支持的依赖清单（包括 uv.lock）标记覆盖缺口；不执行漏洞载荷或动态验证；原生 AI 自然结束不代表逐文件、全调用链或全部业务逻辑审查完成。0.8.0 不含 benchmark 验证。

结束后关闭本次浏览器页，停止拥有的 3198 Host 和合成模型，确认锁释放，恢复隔离 settings.yaml。未更新正式 ~/.dsh，未改动旧 3196 实例，未提交或推送本次变更。
