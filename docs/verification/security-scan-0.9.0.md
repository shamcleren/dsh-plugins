# Security Scan 0.9.0 验证

2026-09-10，macOS arm64，官方 DSH 0.1.0-rc.8。

TypeScript、服务端 / 客户端构建通过。81 项回归全部通过，无跳过，包含真实 Semgrep 与官方 profile 入口。新增检查涵盖扫描时代码留档、工作区改变后留档不变、脚本字符串安全转义、敏感文件整段隐藏、摘要不匹配拒绝保存、中文提交纠正与原生取证片段返回；既有原生 Agent / 任务 / 报告生命周期测试继续通过。

0.9.0 包安装到独立 profile。以 3198 正式 Web 入口打开真实 Semgrep 扫描三文件合成仓库产生的报告，点击 app.py:4，HTML iframe 内 dialog 显示原文件 SHA-256、附近代码和第 4 行高亮。未打开 VS Code / Cursor，没有调用 source RPC；查看器只读取 HTML 内的归档 JSON。独立示例 HTML 在可视化目录保留，不依赖源代码目录或运行中的 Host。因浏览器 URL 策略限制，未自动打开 file:// 页面；交互在独立 Host 的 sandbox iframe 中验证，离线机制另经源码和回归验证。

发布包 74 个文件与构建及最终测试安装内容一致（package.json 比较 JSON 语义），catalog 大小 / 摘要校验通过。SHA-256：3ca6172845bff5a82ae842c27491b8190c5df914401cbe24f4b0a5ae5fa0e53f。

本次没有调用真实模型或重扫用户业务仓库；合成模型回归不能证明真实模型中文表述或漏洞判断质量。片段为限量脱敏上下文，不声称保存整个仓库。旧报告的缺失代码或未翻译英文复核不会自动补造。

关闭本次验收页并停止拥有的 3198 Host，未更新正式 App 或原始用户报告，未提交推送。
