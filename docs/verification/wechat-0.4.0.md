# 微信插件 0.4.0 验证

2026-09-10，macOS arm64，官方 DSH 0.1.0-rc.8，App 内置 Node 24.19.0。

- TypeScript、服务端和客户端构建通过；37 项测试通过，无跳过。覆盖已有绑定在加载时自动启动、旧 enabled/accountIds 配置不阻止接收、只读设置无需写入、扫码管理员自动识别、陌生用户聊天、管理命令拒绝、访客审批保留 Host 处理、卸载清理和工作区与凭据目录分离。
- 0.4.0 tarball 经仓库插件安装流程进入独立 profile，正式 DSH Web 入口运行在 3198。测试 profile 保留 enabled=false 和过期 ID 筛选，预置一个模拟已绑定账号；没有再次扫码，也没有修改其开关。
- 测试进程的 fetch 夹具拦截微信与模型请求，只接受模拟凭据。一个非扫码用户的模拟消息触发了接收、原生 Session、原生模型响应和模拟回复发送，游标及回复上下文按既有机制持久化。没有向真实微信发送内容。
- 原生 UI 的微信卡片仅有账号绑定、添加账号、刷新状态，显示“已绑定”和“消息接收服务运行中”；不再出现启用、用户 ID、管理员 ID、账号筛选或参数表单。微信机器人工作区中可以打开真实生成的 session，看到入站文本和模型响应。

验收证明发布包的接线、默认行为和权限边界，不替代真实微信账号及网络连通性验收。用户正式 ~/.dsh 和原 App 未修改。验收后停止本次 Host、移除本次模拟账号并恢复隔离 profile 原设置，持久化测试会话保留供回看。
